import {Request, Response} from "express";
import { ApiResponse } from "../utils/api-response";



export const handelGetBook = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.error(error, "Error fetching data from the database.");
  }
};

